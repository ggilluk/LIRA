[project]
name = "lira"
version = "0.1.0"
description = "LIRA: a tensor-native knowledge graph platform"
requires-python = ">=3.10"
dependencies = [
    "numpy",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
