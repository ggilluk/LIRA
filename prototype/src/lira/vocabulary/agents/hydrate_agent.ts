import { VocabularyAgent } from "./index";

/** Hydrates vocabulary entries with source data (Layer Summary:
 * Vocabulary Layer). Ported from vocabulary/agents/hydrate_agent.py. */
export class HydrateAgent extends VocabularyAgent {}
