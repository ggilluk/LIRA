import { VocabularyAgent } from "./index";

/** Seeds the Vocabulary Layer's lexical inventory (Layer Summary:
 * Vocabulary Layer). Ported from vocabulary/agents/seed_agent.py. */
export class SeedAgent extends VocabularyAgent {}
