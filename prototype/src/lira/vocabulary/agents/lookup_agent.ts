import { VocabularyAgent } from "./index";

/** Looks up entries in the Vocabulary Layer's lexical inventory (Layer
 * Summary: Vocabulary Layer). Ported from
 * vocabulary/agents/lookup_agent.py. */
export class LookupAgent extends VocabularyAgent {}
