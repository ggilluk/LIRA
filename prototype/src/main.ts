const app = document.querySelector<HTMLDivElement>("#app");
if (app) {
  app.innerHTML = `
    <h1>LIRA</h1>
    <p>Standalone, browser-only TypeScript prototype -- scaffolding only, no layers implemented yet.</p>
  `;
}
